# Impact Engine Configuration
# Clean, readable YAML format following simulator config style

DATA:
  TYPE: simulator
  MODE: rule
  SEED: 42

MEASUREMENT:
  MODEL: interrupted_time_series
  PARAMS:
    DEPENDENT_VARIABLE: revenue
    INTERVENTION_DATE: "2024-11-15"
    START_DATE: "2024-11-01"
    END_DATE: "2024-11-30"

OUTPUT:
  PATH: demo/impact/results